## 2024-05-22 - Missing Form Labels in Modals
**Learning:** Modals with form inputs (like Settings) consistently missed `htmlFor`/`id` associations, relying on visual proximity which fails for screen readers.
**Action:** Check all new modals for proper label association.
